"""BLA — Bicameral Latent Architecture (System-1 + planning).

Subpackages:
    bla.recipes  — locked recipes A/B/C/D/E with their hyperparameters
    bla.routing  — Recipe Router: TaskDescriptor → Recipe (deployment doctrine)

Existing code under `system1_jepa/`, `system2_dca/`, `scripts/` is the
research-arc layout. New code added under `bla/` is the Scale-0
packaging of the doctrine.

See `docs/BLA_SYSTEM1_WORLD_MODEL_ARCHITECTURE.md` and
`docs/BLA_SCALING_ROADMAP.md` for the surrounding spec.
"""
